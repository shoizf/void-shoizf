# Omarchy core behaviour + Tokyo-Night colors (mako syntax)

anchor=top-right
default-timeout=5000
width=420
outer-margin=20
padding=10,15
border-size=2
max-icon-size=32
font=sans-serif 14px
max-visible=3
margin=10

# Colors (Tokyo-Night)
background-color=#1a1b26
text-color=#c0caf5
border-color=#33ccff

# per-app / mode criteria blocks
[app-name=Spotify]
invisible=true

[mode=do-not-disturb]
invisible=true

[mode=do-not-disturb app-name=notify-send]
invisible=false

[urgency=critical]
default-timeout=0

# example button actions from Omarchy (optional)
[summary~="Setup Wi-Fi"]
on-button-left=exec sh -c 'omarchy-notification-dismiss "Setup Wi-Fi"; omarchy-launch-wifi'

